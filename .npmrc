# @browserbasehq/stagehand and openai declare conflicting peer deps (zod).
# Allow the install to resolve them instead of aborting with ERESOLVE.
# Applies to every npm install, including the Render build.
legacy-peer-deps=true
