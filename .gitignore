node_modules/*
dist/*
.env*
!.env.example
*.db
.DS_Store

# Runtime data (recreated on startup) — keep personal resumes/DB out of git
src/data/

# Local Claude/agent tooling state
.superpowers/

# Extension release zip (build artifact — attached to GitHub Releases)
job-agent-extension-*.zip

# Frontend build output and local env
web/dist/
web/.env.local
web/node_modules/
